type Query {
  surveys(type: String, groups: [String]): [Survey!]!
  survey(id: ID!): Survey
}

type Survey {
  id: ID!
  title: String!
  description: String
  type: String
  typeKeywords: [String]
  # formInfo: FormInfo
}

# type FormInfo {}
